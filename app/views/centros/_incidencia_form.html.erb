<div class="well fields">
	<fieldset>
		<legend>Nueva Incidencia</legend>
		<div class="clearfix">
			<%= label_tag :tipo_anomalia, 'Tipo' %>
			<div class="input">
				<%= collection_select(:anomalia, :tipo_anomalia_id, TipoAnomalia.all, :id, :descripcion, :include_blank => 'Seleccione') %>
			</div>
		</div>
		<div class="clearfix">
			<%= f.label :anomalia_id, 'Causa' %>
			<div class="input">
				<%= f.collection_select(:anomalia_id, Anomalia.all, :id, :descripcion, :include_blank => 'Seleccione') %>
			</div>
		</div>
		<div class="clearfix">
			<%= f.label :descripcion, 'Descripción' %>
			<div class="input">
				<%= f.text_area :descripcion, :rows => 5, :class => 'descripcion_incidencia' %>
			</div>
		</div>
		<div class="input">
			<%= link_to_function 'Guardar Incidencia', 'guardar_y_esconder(this.parentNode.parentNode.parentNode)', :class => ['btn', 'primary', 'small'] %>
			<%= link_to_remove_fields 'Cancelar', f %>
		</div>
	</fieldset>
</div>