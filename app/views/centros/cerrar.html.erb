	<h1 id="ultima_llamada">&Uacute;ltima Llamada <small> <%= @centro.nombre %> </small></h1>
</div>
<%= form_for @centro do |f| %>
<div class="row">
	<div class="span1"> &nbsp; </div>
	<div class="span6">
		<fieldset>
			<div class="clearfix">
				<%= f.label :cerro, "¿Cerró el centro?" %>
				<div class="input">
					<ul class="inputs-list">
						<li>
							<label>
								<%= f.radio_button :cerro, Time.now, :id => 'centro_cerro' %>
								<span><%= 'Sí' %></span>
							</label>
						</li>
						<li>
							<label>
								<%= f.radio_button :cerro, false %>
								<span><%= 'No' %></span>
							</label>
						</li>
					</ul>
				</div>
			</div>
		
			<div class="clearfix">
				<%= f.label :escrutinio, "¿Se hizo el escrutinio?" %>
				<div class="input">
					<ul class="inputs-list">
						<li>
							<label>
								<%= f.radio_button :escrutinio, Time.now, :id => 'centro_escrutinio' %>
								<span><%= 'Sí' %></span>
							</label>
						</li>
						<li>
							<label>
								<%= f.radio_button :escrutinio, false %>
								<span><%= 'No' %></span>
							</label>
						</li>
					</ul>
				</div>
			</div>

			<%= f.fields_for :mesas do |builder| %>
			<%= render 'mesas_form', :f => builder %>
			<% end %>


		</fieldset>
	</div><!-- end span6 -->
	<div class="span7">
		<%= f.fields_for :incidencias, f.object.class.reflect_on_association(:incidencias).klass.new do |builder| %>
		<%= render 'incidencia_form', :f => builder %>
		<% end %>

		<p id="link_add" ><%= link_to_add_fields 'Agregar Nueva Incidencia', f, :incidencias %></p>
	</div>	
</div><!-- end row -->
<div class="row">
	<div class="span14">
		<div class="actions">
			<%= f.submit "Guardar", :disable_with => 'Guardando...', :class => 'btn primary'%>
			<button type="reset" class="btn"> Cancelar </button>
		</div>
	</div>
</div>
<% end %>
